/**
 * clientTaskOpen — no longer used.
 * Kept as an empty export to avoid stale import errors until all
 * references are cleaned up.
 */
export default function ClientTaskDetails() {
  return null;
}
